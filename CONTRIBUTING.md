# 參與貢獻

感謝協助維護秤心育兒指南。這個專案處理孕產、醫療提醒與補助資料；任何改動都應優先確保來源可追溯、文字不誇大，且不要求使用者提供敏感個人資料。

## 開發環境

- Node.js 20 以上。
- 不需要安裝第三方套件。
- 執行 `npm run serve` 後，以瀏覽器開啟 `http://127.0.0.1:4173/`。

## 分支與提交

- 新功能：`feature/xxx`
- 修正：`fix/xxx`
- 維護：`chore/xxx`
- Commit 使用 Conventional Commits，描述必須為繁體中文，例如 `feat: 加入托育方式篩選`、`fix: 修正跨年孕週計算`。
- 不得提交或推送 `.env`、憑證、token、私人金鑰、真實家庭個資或醫療紀錄。

## 內容更新流程

1. 優先使用法規、中央或臺北市主管機關頁面；新聞與第三方整理只可作為找來源的線索。
2. 核對金額、資格、排除條件、互斥關係、申請期限、管道、生效日與最後查核日。
3. 在 `js/data/benefits.js` 更新資料與來源；不得只改畫面上的數字。
4. 政策尚未生效、家庭條件不足或規則有例外時，使用「需要確認」，不可宣稱「符合」。
5. 開放資料快照更新時，同步更新 `js/data/open-data-fallback.js` 的完整資料、檔案日期、詮釋資料日期及查核日期。
6. 在 `docs/TEST-PLAN.md` 留下手動查核項目，必要時補上回歸測試。

## 程式碼原則

- 維持 HTML、CSS、原生 JavaScript 與 ES Modules，不引入建構流程或大型框架。
- 領域邏輯保持為可在 Node.js 測試的純函式；DOM 事件集中於 `js/app.js`。
- 使用者輸入必須以 `textContent` 或安全文字節點渲染，不可插入 `innerHTML`。
- 所有站內 `href`、`src` 與 manifest 路徑使用相對路徑，確保 GitHub Pages 子路徑相容。
- 新互動要有鍵盤操作、可辨識名稱、焦點樣式與不只依顏色傳達的狀態。

## 送出變更前

```powershell
npm test
npm run validate
git diff --check
```

再依 `docs/TEST-PLAN.md` 於手機、平板與桌機寬度檢查主要流程。若修正錯誤，先加入能重現問題的失敗測試，再實作修正。
